{
  "kind": "collectionType",
  "collectionName": "quizzes",
  "info": {
    "singularName": "quiz",
    "pluralName": "quizzes",
    "displayName": "Quiz"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "passingScore": {
      "type": "integer",
      "required": true
    },
    "course": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::course.course",
      "required": true,
      "inversedBy": "quizzes"
    },
    "questions": {
      "type": "component",
      "component": "quiz-elements.question",
      "repeatable": true,
      "required": true
    },
    "quiz_submissions": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::quiz-submission.quiz-submission",
      "mappedBy": "quiz"
    }
  }
}
